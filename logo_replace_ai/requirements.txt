# --- always required -------------------------------------------------------
# The classical pipeline (--inpaint classical / none, --boxes) runs on these
# two alone: no GPU, no model downloads, a few seconds to install.
pillow>=10.2,<12
numpy>=1.26,<3

# --- optional: alternative classical fills ---------------------------------
# --classical-method telea|ns. The default 'pillow' fill needs no OpenCV, and
# on Windows a cv2 wheel can import-fail with "DLL load failed" when the
# Visual C++ runtime is absent — which is why it is not a hard requirement.
# opencv-python>=4.9,<5

# --- optional: text detection and replacement ------------------------------
# --find-text / --set-text. pytesseract is only a wrapper — the Tesseract
# engine is a separate program you must install yourself:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki (tick "Add to PATH")
#   Debian/Ubuntu: sudo apt install tesseract-ocr
# pytesseract>=0.3.10

# --- YOLO detection --------------------------------------------------------
# Needed for automatic logo detection (anything except --boxes).
# ultralytics pulls in torch; on Windows/Linux with an NVIDIA GPU install the
# CUDA build of torch FIRST, otherwise pip gives you the CPU-only wheel:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
ultralytics>=8.2,<9
torch>=2.2

# --- Stable Diffusion XL inpainting ---------------------------------------
# Needed for --inpaint sdxl (the default). First run downloads ~7 GB.
diffusers>=0.27,<1
transformers>=4.38
accelerate>=0.27
safetensors>=0.4
